langcode: en
status: true
dependencies:
  config:
    - group.type.release
  module:
    - user
id: release-group_membership
label: 'Release: Group membership'
description: 'Adds users to groups as members.'
group_type: release
content_plugin: group_membership
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: true
