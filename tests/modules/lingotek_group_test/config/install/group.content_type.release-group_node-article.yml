langcode: en
status: true
dependencies:
  config:
    - group.type.release
    - node.type.article
  module:
    - gnode
    - node
id: release-group_node-article
label: 'Release: Group node (Article)'
description: 'Adds <em class="placeholder">Article</em> content to groups both publicly and privately.'
group_type: release
content_plugin: 'group_node:article'
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: true
